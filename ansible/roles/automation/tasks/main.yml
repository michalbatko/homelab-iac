- name: Install required packages
  apt:
    name:
      - sudo
      - unattended-upgrades
      - apt-listchanges
      - curl
      - unzip
      - git
      - software-properties-common
      - gnupg
      - terraform
      - ansible
      - nodejs
      - npm
    state: present
    update_cache: yes
