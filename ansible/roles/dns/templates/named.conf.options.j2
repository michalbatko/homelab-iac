options {
        directory "/var/cache/bind";
        dnssec-validation no;
        forwarders {
{% for forwarder in default_forwarders %}
                {{ forwarder }};
{% endfor %}
        };
        forward only;
        listen-on { any; };
};
