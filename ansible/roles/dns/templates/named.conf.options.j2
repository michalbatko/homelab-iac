options {
        directory "/var/cache/bind";
        dnssec-validation auto;
        forwarders {
{% for forwarder in default_forwarders %}
                {{ forwarder }};
{% endfor %}
        };
        forward only;
        listen-on-v6 { any; };
};
